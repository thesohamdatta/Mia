---
name: {{SKILL_NAME}}
preamble-tier: {{PREAMBLE_TIER}}
version: {{VERSION}}
description: {{DESCRIPTION}}
allowed-tools:
{{#each ALLOWED_TOOLS}}
  - {{this}}
{{/each}}
triggers:
{{#each TRIGGERS}}
  - {{this}}
{{/each}}
---

{{PREAMBLE}}

## When to invoke this skill

{{WHEN_TO_INVOKE}}

## Workflow

{{WORKFLOW}}

---

*Auto-generated from skill template. Run `mia gen:skill-docs` to regenerate.*